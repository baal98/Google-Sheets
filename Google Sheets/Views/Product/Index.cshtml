@model IEnumerable<Google_Sheets.Data.Models.Product>

@foreach (var product in Model)
{
    <div>
        <h2>@product.Name</h2>
        <p>@product.Description</p>
        <p>@product.Price</p>
    </div>
}